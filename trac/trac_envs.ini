[common]
database_user = user
database_user_password = userpwd

[trac_instance]
agilo=1
title = trac_instance
url = /trac/trac_instance
project = /var/www/trac/trac_instance
email2trac = trac_instance
database = trac_trac_instance
